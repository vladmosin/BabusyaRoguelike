syntax = "proto3";

option java_package = "inc.roguelike.babusya.network";

message Empty {}

message Message {
    string level = 1;
    bool ends = 2;
    string log = 3;
}

service Game {
    rpc receiveMessage (Empty) returns (stream Message);
    rpc sendMessage (Message) returns (Empty);
}
